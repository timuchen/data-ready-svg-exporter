{
  "name": "data-ready-svg-exporter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build:plugin",
    "build:plugin": "node ./scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2"
  }
}
